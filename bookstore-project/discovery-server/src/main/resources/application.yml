
server:
  port: 8761

spring:
  application:
    name: discovery-server
#eureka config
  eureka:
    instance:
      hostname: localhost
    client:
      register-with-eureka: false
      fetch-registry: false



